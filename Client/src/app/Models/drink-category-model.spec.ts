import { DrinkCategoryModel } from './drink-category-model';

describe('DrinkCategoryModel', () => {
  it('should create an instance', () => {
    expect(new DrinkCategoryModel()).toBeTruthy();
  });
});
